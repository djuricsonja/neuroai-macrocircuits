after_training: null
agent: tonic.torch.agents.PPO(model=ppo_swimmer_model(n_joints=5, critic_sizes=(256,
  256), action_noise=0.1, use_weight_sharing=True), actor_updater=tonic.torch.updaters.ClippedRatio(gradient_clip=0.5),
  critic_updater=tonic.torch.updaters.VRegression(gradient_clip=0.5))
before_training: null
environment: tonic.environments.ControlSuite("swimmer-swim_to_ball", time_feature=True)
header: null
name: ncap_ppo
parallel: 1
seed: 0
sequential: 1
trainer: tonic.Trainer(steps=500000, epoch_steps=20000, save_steps=50000)
