after_training: null
agent: tonic.torch.agents.DDPG(model=ddpg_mlp_model(actor_sizes=(256, 256), critic_sizes=(256,
  256)), actor_updater=tonic.torch.updaters.DeterministicPolicyGradient(gradient_clip=0.5),
  critic_updater=tonic.torch.updaters.DeterministicQLearning(gradient_clip=0.5), exploration=tonic.explorations.NormalActionNoise(scale=0.1))
before_training: null
environment: 'tonic.environments.ControlSuite("swimmer-foraging", task_kwargs={''progress_reward_weight'':
  10.0, ''eat_bonus'': 5.0})'
header: import tonic.torch
name: mlp_ddpg_forage_poc
parallel: 1
seed: 0
sequential: 1
trainer: tonic.Trainer(steps=100000, epoch_steps=20000, save_steps=50000)
