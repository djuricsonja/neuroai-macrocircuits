after_training: null
agent: tonic.torch.agents.PPO(model=ppo_swimmer_model(n_joints=5, critic_sizes=(256,
  256), action_noise=0.1, controller=make_learned_steering(5)), actor_updater=tonic.torch.updaters.ClippedRatio(gradient_clip=0.5),
  critic_updater=tonic.torch.updaters.VRegression(gradient_clip=0.5))
before_training: null
environment: 'tonic.environments.ControlSuite("swimmer-foraging", time_feature=True,
  task_kwargs={''progress_reward_weight'': 10.0, ''eat_bonus'': 5.0})'
header: import tonic.torch
name: ls_progress_eat_sanity_seed0
parallel: 1
seed: 0
sequential: 1
trainer: tonic.Trainer(steps=30000, epoch_steps=6000, save_steps=50000)
