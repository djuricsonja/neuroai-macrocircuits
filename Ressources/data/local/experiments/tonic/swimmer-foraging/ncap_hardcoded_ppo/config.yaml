after_training: null
agent: tonic.torch.agents.PPO(model=ppo_swimmer_model(n_joints=5, critic_sizes=(256,
  256), action_noise=0.1, controller=make_steer_to_food_reflex(5), include_speed_control=True),
  actor_updater=tonic.torch.updaters.ClippedRatio(gradient_clip=0.5), critic_updater=tonic.torch.updaters.VRegression(gradient_clip=0.5))
before_training: null
environment: 'tonic.environments.ControlSuite("swimmer-foraging", time_feature=True,
  task_kwargs={''speed_reward_weight'': 0.0, ''alignment_reward_weight'': 0.5})'
header: import tonic.torch
name: ncap_hardcoded_ppo
parallel: 1
seed: 0
sequential: 1
trainer: tonic.Trainer(steps=100000, epoch_steps=20000, save_steps=50000)
